# ringbuffer-c
Ring buffer (fila circular) em C11: biblioteca enxuta para streaming de eventos de alta vazao, com API clara, testes proprios e CI (gcc + clang, -Wall -Werror, ASan). Casos: fintech e telemetria.
